%h1= t('songs.new_song')

%p{:id=>"tsandcs"}
  = t('read')
  = succeed "?" do
    %a{:href => root_path}
      = t('tsandcs')
%input{:type => "checkbox", :id => "tsandcsCheck"}/

= render 'form'

= link_to t('songs.view_all'), songs_path
